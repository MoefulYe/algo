// url = https://sunnywhy.com/camp/2304/model/2592?itemId=2623

#include <bits/stdc++.h>

auto n = 0, m = 0, a = 0, b = 0, c = 0;
auto graph = vector<vector<int>>{};

auto init() {
    cin >> n >> m >> a >> b >> c;
    graph = vector<vector<int>>(n+1);
    for(auto _ = 0; _ < m; _++) {
        auto u = 0, v = 0;
        
    }
}

auto dist(int src, int dest) -> optional<int> {
    auto visited = vector<bool>{};
   
}

auto main() -> int {

}